"use strict";
/* // Type inférence

let firstName;
firstName = true;
firstName = 'red';
firstName = 10;

// Vérouillage type par inférence
// String :
let lastName = "Dupont";

// Number :
let age =20;

//Boolean :
let isLogging = true;

//Modifiable si même type
lastName = 'Dura';
age = 25;
isLogging = false;

//Sinon error:

lastName = 20;
age = true;
isLogging = 'capjfiojfaz';

// Type assignation :

let alien: string = 20; // ==> Allow only the type defined

let width: number;
width = 'hello';

// Functions :
const sum = (val1, val2) => {
    return val1 + val2 // NaN -> string + number don't match
}

const summ = (val1:number, val2:number) => {
    return val1 + val2;
}

summ('toto', 15) // => force number type */ 
