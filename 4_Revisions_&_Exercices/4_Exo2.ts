/* let colors: string[] = []; // ou Array<string> = []

colors.push("red")
console.log(colors)

let infos: Array<string | number | boolean> = []; // ou (string | number | boolean)[]

infos.push("dupont", true, 40)

console.log(infos) */